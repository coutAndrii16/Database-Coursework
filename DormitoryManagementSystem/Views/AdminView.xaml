<UserControl x:Class="DormitoryManagementSystem.Views.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DormitoryManagementSystem.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="20">
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#E6F4EA" Offset="0" />
                <!-- дуже світлий зелений -->
                <GradientStop Color="#C7E6C9" Offset="1" />
                <!-- світло-зелений -->
            </LinearGradientBrush>
        </Grid.Background>
        <Grid.RowDefinitions>
            
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Рядок 0: вітальний текст -->
        <TextBlock Grid.Row="0"
                   Text="{Binding WelcomeText}"
                   FontSize="24"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   VerticalAlignment ="Center"
                   Margin="0,100,0,0"/>

        <!-- Рядок 1: кнопка “Адмін: звернення” -->
        <WrapPanel Grid.Row="1"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   ItemHeight="40"
                   ItemWidth="140">

            <Button Style="{StaticResource NavButtonStyle}"
                    Content="Адмін: звернення"
                    Command="{Binding OpenAdminContactsCommand}"/>
        </WrapPanel>
        <!-- Кнопка Вийти -->
        <WrapPanel Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,40,0,10">
            <Button Style="{StaticResource LogoutButtonStyle}" Content="Вийти" Command="{Binding LogoutCommand}" Width="140" Height="40" />
        </WrapPanel>

    </Grid>
</UserControl>
