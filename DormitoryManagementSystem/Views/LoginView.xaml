<UserControl x:Class="DormitoryManagementSystem.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:viewModels="clr-namespace:DormitoryManagementSystem.ViewModels"
             x:Name="ThisControl"
             Background="#E6F4EA">
    <Grid Background="#E6F4EA">
        <Border Width="350" Padding="20" Background="White" CornerRadius="12" 
                VerticalAlignment="Center" HorizontalAlignment="Center"
                Effect="{DynamicResource DropShadow}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Ел. пошта:" Foreground="#2E5939" FontWeight="SemiBold"/>
                <TextBox Grid.Row="1"
                         Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" 
                         Margin="0,5"
                         Padding="6"
                         BorderBrush="#A3C294" BorderThickness="1"
                         Background="#FFF"/>

                <TextBlock Text="Пароль:" Grid.Row="2"
                           Margin="0,10,0,0" Foreground="#2E5939" FontWeight="SemiBold"/>
                <PasswordBox Grid.Row="3"
                             Name="PasswordBox"
                             Margin="0,5"
                             Padding="6"
                             BorderBrush="#A3C294" BorderThickness="1"
                             Background="#FFF"
                             PasswordChanged="PasswordBox_PasswordChanged"/>
                <Button Grid.Row="4" Content="Увійти" Margin="0,15,0,0" 
                        Background="#4CAF50" Foreground="White" FontWeight="Bold"
                        Height="38" Cursor="Hand"
                        Command="{Binding LoginCommand}" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
